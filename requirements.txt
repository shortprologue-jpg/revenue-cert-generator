streamlit>=1.35.0
notion-client>=2.2.1
python-dotenv>=1.0.1
google-genai>=1.0.0
# 웹 배포용 저장소(Supabase). 로컬 실행에는 불필요 — history_store 가 지연 임포트.
supabase>=2.31,<3
# 전달 데이터 이미지 클립보드 붙여넣기(Ctrl+V). 미설치 시 붙여넣기만 비활성(업로드·드래그는 정상).
streamlit-paste-button>=0.1.2
Pillow>=10.0.0
